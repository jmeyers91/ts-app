import { Migration } from '../utils/knexUtils';

const migration: Migration = {
  name: '{{> timestamp}}_{{ snakeCase name }}',

  up(knex) {

  },

  down(knex) {

  },
};

export default migration;
